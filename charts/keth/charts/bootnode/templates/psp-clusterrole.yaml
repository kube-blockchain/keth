{{- if and .Values.global.bootnode.enabled .Values.rbac.pspEnabled }}
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ template "bootnode.fullname" . }}-psp
  labels:
    {{ include "bootnode.labels" . | indent 4 | trim }}
rules:
- apiGroups: ['extensions']
  resources: ['podsecuritypolicies']
  verbs:     ['use']
  resourceNames:
  - {{ template "bootnode.fullname" . }}
{{- end }}
