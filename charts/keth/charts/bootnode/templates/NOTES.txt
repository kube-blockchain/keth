Keth has been installed. Check its status by running:
  kubectl --namespace {{ .Release.Namespace }} get pods -l "app.kubernetes.io/name={{ include "bootnode.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

Visit https://github.com/kube-blockchain/keth for instructions on how to use Keth
